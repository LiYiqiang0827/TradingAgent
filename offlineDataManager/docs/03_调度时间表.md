# 03 调度时间表

> 本文是 offlineDataManager 的**唯一调度时间表**:每天 4 个 task 在哪个时间点跑、跑哪些 service、更新哪些表、写入哪些 SQLite 库。

## 1. 一图看懂(每日调度全景)

```
时间  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

09:05 ┃ task_morning_update    ━ stk_limit + kpl_list + cctv_news
      ┃                          (3 个 service,~3 秒)

      ┃ (09:00-09:30 A 股盘前,用户开盘前看到昨天收盘后的状态)

16:30 ┃ task_kpl_limit_performance ━ service_kpl_limit_performance
      ┃                               (实时补 kpl_list 滞后数据)

      ┃ (15:00 A 股收盘,kpl API 30 分钟后才有当天完整涨停数据)

18:00 ┃ task_full_update 阶段 1-4 ━ 完整更新(basic + daily + week/month + kpl + 涨跌停 + cctv)
20:00 ┃ task_full_update 阶段 1-4 ━ 同上,兜底
22:00 ┃ task_full_update 阶段 1-4 ━ 同上,最终态

每10分 ┃ task_news_update    ━ service_news + service_major_news
       ┃                       (08:00 - 次日 05:00 异步)
```

## 2. 4 个 task 详表

### 2.1 `task_morning_update` — 早上快速更新(09:05)

**目的**:用户开盘前(09:30)看到昨天收盘后的关键状态

| # | Service | 表 | DB | 写入方式 | 耗时 |
|---|---|---|---|---|---|
| 1 | `service_stk_limit` | `tbl_cn_stk_limit` | `db_cn_basic.db` | upsert(增量) | ~0.2s |
| 2 | `service_kpl_list` | `tbl_cn_kpl_list` | `db_cn_kpl.db` | upsert(单日) | ~1s |
| 3 | `service_cctv_news` | `tbl_cctv_news` | `db_cn_news.db` | upsert(增量) | ~0.7s |

**特点**:
- 轻量,~3 秒跑完
- 失败 → 中止不补跑(等 18:00 `task_full_update` 兜底)
- 09:05 触发(比开盘 09:30 早 25 分钟,够完成)

**常量**:`STR_TIME_MORNING = "09:05"`

### 2.2 `task_full_update` — 完整更新(18:00 / 20:00 / 22:00,每天 3 次)

**目的**:全市场数据最终态,**收盘后兜底**

#### 4 阶段

**阶段 1 — 基础数据**(任一失败整体 return)

| # | Service | 表 | DB | 写入方式 |
|---|---|---|---|---|
| 1 | `service_basic` | `tbl_cn_basic` | `db_cn_basic.db` | upsert(全量 DELETE 后) |
| 2 | `service_tradecal` | `tbl_cn_tradecal` | `db_cn_basic.db` | upsert |

**阶段 2 — 日 K 链**(任一失败整体 return)

| # | Service | 表 | DB | 写入方式 |
|---|---|---|---|---|
| 3 | `service_daily` | `tbl_cn_day` | `db_cn_basic.db` | upsert |
| 4 | `service_adj_factor` | `tbl_cn_adj_factor` | `db_cn_basic.db` | upsert |

**阶段 3 — 周月 K**(任一失败整体 return)

| # | Service | 表 | DB | 写入方式 |
|---|---|---|---|---|
| 5 | `service_week` | `tbl_cn_week` | `db_cn_basic.db` | replace_table(全量) |
| 6 | `service_month` | `tbl_cn_month` | `db_cn_basic.db` | replace_table(全量) |

**阶段 4 — kpl + 涨跌停 + 停复牌 + 龙虎榜 + 大宗 + 港股通 + 沪深股通 + 涨跌停列表 + 资金流向 + 融资融券 + 融资融券明细 + 筹码及胜率 + 每日指标 + 指数基本信息 + 指数日线行情 + cctv**(任一失败只 logger.error,继续下一项)

| # | Service | 表 | DB | 写入方式 |
|---|---|---|---|---|
| 7 | `service_kpl_list` | `tbl_cn_kpl_list` | `db_cn_kpl.db` | upsert |
| 8 | `service_kpl_concept_cons` | `tbl_cn_kpl_concept_cons` | `db_cn_kpl.db` | upsert |
| 9 | `service_kpl_limit_performance` | `tbl_cn_kpl_limit_performance` | `db_cn_kpl.db` | upsert |
| 10 | `service_stk_limit` | `tbl_cn_stk_limit` | `db_cn_basic.db` | upsert |
| 11 | `service_suspend` | `tbl_cn_suspend` | `db_cn_basic.db` | upsert(按月循环) |
| 12 | `service_top_list` | `tbl_cn_top_list` | `db_cn_kpl.db` | upsert(按日循环) |
| 13 | `service_top_inst` | `tbl_cn_top_inst` | `db_cn_kpl.db` | upsert(按日循环) |
| 14 | `service_block_trade` | `tbl_cn_block_trade` | `db_cn_kpl.db` | upsert(按月 + OFFSET 分页) |
| 15 | `service_ggt_daily` | `tbl_cn_ggt_daily` | `db_cn_kpl.db` | upsert(按月) |
| 16 | `service_hsgt_top10
service_limit_list` | `tbl_cn_hsgt_top10` | `db_cn_kpl.db` | upsert(按月) |
| 17 | `service_moneyflow` | `tbl_cn_moneyflow` | `db_cn_basic.db` | upsert(按月 + OFFSET 分页) |
| 18 | `service_margin` | `tbl_cn_margin` | `db_cn_basic.db` | upsert(按日,task_morning) |
| 19 | `service_margin_detail` | `tbl_cn_margin_detail` | `db_cn_basic.db` | upsert(按月+OFFSET,task_morning) |
| 20 | `service_cyq_perf` | `tbl_cn_cyq_perf` | `db_cn_basic.db` | upsert(按日,数据 18-21 点更新) |
| 21 | `service_daily_basic` | `tbl_cn_daily_basic` | `db_cn_basic.db` | upsert(按日,数据 15-17 点更新) |
| 22 | `service_index_basic` | `tbl_cn_index_basic` | `db_cn_index.db` | **replace_table**(覆盖,~30 秒) |
| 23 | `service_index_daily` | `tbl_cn_index_daily` | `db_cn_index.db` | upsert(按 ts_code 循环 + limit/offset) |
| 24 | `service_cctv_news` | `tbl_cctv_news` | `db_cn_news.db` | upsert |

**特点**:
- 4 阶段顺序执行,严格依赖链:`basic/tradecal` → `daily/adj_factor` → `week/month`(内部校验) → kpl/涨跌停/cctv(独立)
- 阶段 1-3 任一失败 → 整体中止,阶段 4 单项失败不影响其他
- 单次完整跑 ~5-15 分钟(取决于网络和限流)
- 18:00 是收盘后第一次补,20:00/22:00 是晚间兜底

**常量**:`STR_TIME_FULL_DB_1 = "18:00"` / `_2 = "20:00"` / `_3 = "22:00"`

### 2.3 `task_kpl_limit_performance` — 涨停表现详情(16:30 / 20:00)

**目的**:实时补 `kpl_list` 的滞后数据(开盘啦 API 16:00 后才有当天完整涨停)

| # | Service | 表 | DB | 写入方式 |
|---|---|---|---|---|
| 1 | `service_kpl_limit_performance` | `tbl_cn_kpl_limit_performance` | `db_cn_kpl.db` | upsert(增量) |

**特点**:
- 每天 2 次(16:30 收盘后 + 20:00 晚间再补)
- 同步 spawn,失败只 logger.error 继续
- 单次 ~1 秒

**常量**:`STR_TIME_KPL_LP_1 = "16:30"` / `STR_TIME_KPL_LP_2 = "20:00"`

### 2.4 `task_news_update` — News 高频更新(每 10 分钟)

**目的**:实时抓快讯(开盘啦 / 财联社等)

| # | Service | 表 | DB | 写入方式 |
|---|---|---|---|---|
| 1 | `service_news` | `tbl_news`(7 个源) | `db_cn_news.db` | upsert + md5(content) |
| 2 | `service_major_news` | `tbl_major_news` | `db_cn_news.db` | upsert + md5(title) |

**特点**:
- **异步 spawn**(不等返回,高频任务)
- 只在 `NEWS_START` (08:00) 到 `NEWS_END` (次日 05:00) 之间跑
- `is_news_window()` 控制时段
- 单次 ~1-3 秒

**常量**:`NEWS_INTERVAL_MIN = 10`(每 10 分钟)

## 3. 完整时间表(总览)

| 时间 | Task | Service 数 | 子进程模式 | 写入表 |
|---|---|---|---|---|
| **09:05** | `task_morning_update` | 3 | 同步 | `tbl_cn_stk_limit` / `tbl_cn_kpl_list` / `tbl_cctv_news` |
| 16:30 | `task_kpl_limit_performance` | 1 | 同步 | `tbl_cn_kpl_limit_performance` |
| **18:00** | `task_full_update` | 11 | 同步 | 阶段 1-4 全套(11 表) |
| 20:00 | `task_kpl_limit_performance` | 1 | 同步 | `tbl_cn_kpl_limit_performance` |
| **20:00** | `task_full_update` | 11 | 同步 | 阶段 1-4 全套(11 表) |
| **22:00** | `task_full_update` | 11 | 同步 | 阶段 1-4 全套(11 表) |
| 每 10 分钟(08:00-05:00) | `task_news_update` | 2 | **异步** | `tbl_news` / `tbl_major_news` |

**总写入表**:15 张(3 库全覆盖) — 见 [01_数据详细设计文档.md](01_数据详细设计文档.md)

## 4. 同步 vs 异步 spawn

| 模式 | 用在哪 | 行为 |
|---|---|---|
| **同步**(`sync=True`) | 阶段 1-4 全部 + 涨停表现 + morning | `subprocess.run` 阻塞,等子进程退出,捕获 rc(0/1/2) |
| **异步**(`sync=False`) | `task_news_update` | `subprocess.Popen` 后台启动,不等返回(高频任务不阻塞调度循环) |

## 5. 失败处理(3 种 rc)

| rc | 含义 | scheduler 处理 |
|---|---|---|
| **0** | 成功 | 继续下一项 |
| **2** | 业务中止(如 week/month ctrl 一致性校验失败) | `raise RuntimeError`,中止后续阶段 |
| 其他(1/其他) | 异常退出 | `raise RuntimeError`,中止后续阶段 |

**特殊情况**:
- `task_morning_update`:任一 service 失败 → **中止整个 task**(不补跑)
- `task_full_update` 阶段 1-3:任一 service 失败 → **中止整个 task**
- `task_full_update` 阶段 4 / `task_kpl_limit_performance` / `task_news_update`:任一 service 失败 → **只 logger.error 继续下一项**

## 6. 启动时行为

scheduler daemon 启动后(`launchd 拉起`):
1. 立即跑一次 `task_full_update`(保证新启动后数据是新的)
2. 注册 `schedule.every().day.at(...)` 4 类任务
3. 进入主循环 `while True: schedule.run_pending(); time.sleep(30)`

## 7. 完整跑一遍的覆盖表

**一次完整 `task_full_update`(18:00)** 会更新这些表(21 张):

```
db_cn_basic.db:
  - tbl_cn_basic           (阶段 1)
  - tbl_cn_tradecal        (阶段 1)
  - tbl_cn_day             (阶段 2)
  - tbl_cn_adj_factor      (阶段 2)
  - tbl_cn_week            (阶段 3,replace)
  - tbl_cn_month           (阶段 3,replace)
  - tbl_cn_stk_limit       (阶段 4)
  - tbl_cn_suspend         (阶段 4,2026-09-15 新增)
  - tbl_cn_top_list        (阶段 4,2026-09-15 新增)
  - tbl_cn_top_inst        (阶段 4,2026-09-15 新增)
  - tbl_cn_block_trade     (阶段 4,2026-09-15 新增)
  - tbl_cn_ggt_daily        (阶段 4,2026-09-15 新增)
  - tbl_cn_hsgt_top10      (阶段 4,2026-09-15 新增)
  - tbl_cn_moneyflow      (阶段 4,2026-09-15 新增)
  - tbl_cn_margin         (阶段 4,2026-09-15 新增,task_morning)
  - tbl_cn_margin_detail  (阶段 4,2026-09-15 新增,task_morning)
  - tbl_cn_cyq_perf       (阶段 4,2026-09-15 新增,数据 18-21 点更新)
  - tbl_cn_daily_basic    (阶段 4,2026-09-15 新增,数据 15-17 点更新)
  - tbl_cn_index_basic    (阶段 4,2026-09-15 新增,**db_cn_index.db**,覆盖更新)
  - tbl_cn_index_daily     (阶段 4,2026-09-15 新增,**db_cn_index.db**,12 只指数日线)

db_cn_kpl.db:
  - tbl_cn_kpl_list              (阶段 4)
  - tbl_cn_kpl_concept_cons      (阶段 4)
  - tbl_cn_kpl_limit_performance (阶段 4 + 16:30 task 也会跑)

db_cn_news.db:
  - tbl_cctv_news          (阶段 4)
```

**加上 `task_news_update` 跑的**:`tbl_news` / `tbl_major_news`

**所有 31 张表都覆盖了** ✓

## 8. 一句话总结

**offlineDataManager 通过 4 个 task(morning / full / kpl_limit_performance / news)在 4 个时间点(09:05 / 16:30 / 18:00 / 20:00 / 22:00 / 每 10 分钟)按依赖链更新 15 张数据表,早上轻量快速,收盘后完整兜底,夜间高频抓新闻。**
