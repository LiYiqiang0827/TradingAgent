# 01 数据详细设计文档

> 本文是 4 个 SQLite 库 + 31 张表的**唯一 schema 文档**(含 4 个 ctrl 断点表 + 27 张数据表)。每个表包含:字段语义、类型、主键、索引、来源、写入者、读取者。
> 数据规模为 2026-09-15 实测。

---

## 1. 总览

| DB | 路径 | 大小 | 表数 | 用途 |
|---|---|---|---|---|
| `db_cn_basic.db` | `data/db_cn_basic.db` | 3.6 GB | 14 | 基础数据(basic / tradecal / 日K / adj_factor / week / month / stk_limit / suspend / moneyflow / margin / margin_detail / cyq_perf / daily_basic) + 断点 |
| `db_cn_kpl.db` | `data/db_cn_kpl.db` | 1.1 GB | 10 | 开盘啦(kpl_list / kpl_concept_cons / kpl_limit_performance / top_list / top_inst / block_trade / ggt_daily / hsgt_top10 / limit_list) + 断点 |
| `db_cn_news.db` | `data/db_cn_news.db` | 6.4 GB | 4 | 新闻(tbl_news / tbl_major_news / tbl_cctv_news) + 源断点 |
| `db_cn_index.db` | `data/db_cn_index.db` | 8 MB | 3 | 指数(tbl_cn_index_basic / tbl_cn_index_daily) + 断点(2026-09-15 新增) |

**4 个 DB,31 张表**(2026-09-15 新增 index DB):
- 13 个 basic 数据表(`tbl_cn_basic` / `tbl_cn_tradecal` / `tbl_cn_day` / `tbl_cn_adj_factor` / `tbl_cn_week` / `tbl_cn_month` / **`tbl_cn_moneyflow`** 资金流向 / **`tbl_cn_stk_limit`** 涨跌停 / **`tbl_cn_suspend`** 停复牌 / **`tbl_cn_margin`** 融资融券汇总 / **`tbl_cn_margin_detail`** 融资融券明细 / **`tbl_cn_cyq_perf`** 筹码及胜率 / **`tbl_cn_daily_basic`** 每日指标)
- 1 个 basic 断点表(`tbl_basic_ctrl`)
- 9 个 kpl 数据表(`tbl_cn_kpl_list` / `tbl_cn_kpl_concept_cons` / `tbl_cn_kpl_limit_performance` / `tbl_cn_top_list` / `tbl_cn_top_inst` / `tbl_cn_block_trade` / `tbl_cn_ggt_daily` / `tbl_cn_hsgt_top10` / `tbl_cn_limit_list` 涨跌停列表)
- 1 个 kpl 断点表(`tbl_kpl_ctrl`,2026-09-15 重建)
- 3 个 news 数据表(`tbl_news` / `tbl_major_news` / `tbl_cctv_news`)
- 1 个 news 断点表(`tbl_news_ctrl`)
- 2 个 index 数据表(2026-09-15 新增:`tbl_cn_index_basic` 指数基本信息 / **`tbl_cn_index_daily`** 12 只指数日线行情)
- 1 个 index 断点表(`tbl_index_ctrl`,2026-09-15 新建)

**Schema 声明**:`scripts/config/settings.py` 四个 `SCHEMA_SQL_*` 变量(运行时通过 `init_db(db_name)` 调 `conn.executescript(schema_sql)` 建表)。

---

## 2. db_cn_basic.db (3.6 GB, 14 表)

### 2.1 `tbl_cn_basic` — 股票基本信息(全量)

| 项 | 值 |
|---|---|
| 主键 | `ts_code` |
| 写入者 | `CNDataDown.update_basic()` |
| 读取者 | `get_basic()` |
| 数据源 | tushare `pro.stock_basic`(list_status=L+P,**不含 D 退市**) |
| 数据行数 | 5,562 |

**字段(15)**:

| 字段 | 类型 | 语义 |
|---|---|---|
| `ts_code` | TEXT PK | 股票代码,带后缀(.SH/.SZ/.BJ) |
| `symbol` | TEXT | 纯数字代码,如 `000001` |
| `name` | TEXT | 简称,如 `平安银行` |
| `industry` | TEXT | 所属行业(申万),**可读但 `get_basic` 不支持 industry 过滤** |
| `fullname` | TEXT | 公司全称 |
| `enname` | TEXT | 英文名 |
| `cnspell` | TEXT | 中文拼音缩写 |
| `market` | TEXT | 板块:`主板` / `创业板` / `科创板` / `北交所`(对应关系见下表) |
| `exchange` | TEXT | 交易所:`SSE` / `SZSE` / `BSE` |
| `curr_type` | TEXT | 币种,基本都是 `CNY` |
| `list_status` | TEXT | `L` 上市 / `P` 暂停 / `D` 退市(DB 只存 L+P) |
| `list_date` | TEXT | 上市日期 YYYYMMDD(字符串排序 == 时间排序,可直接用 `WHERE list_date >= '20240101'`)|
| `delist_date` | TEXT | 退市日期 YYYYMMDD(D 状态有) |
| `is_hs` | TEXT | 沪深港通标记 `N` / `H` / `S`(H 沪股通 / S 深股通) |
| `snap_ts` | TEXT | 写入时间 ISO 格式 |

**索引**:主键 ts_code,无其他索引(全表才 5 千行,过滤靠内存)

**重要约束**:
- `exchange` (SSE/SZSE/BSE) 和 `market` (主板/创业板/科创板/北交所) **不是任意组合**:
  - SSE = 主板(部分)+ 科创板
  - SZSE = 主板(部分)+ 创业板
  - BSE = 北交所
  - 即:"沪市主板+科创板=整个沪市","深市主板+创业板=整个深市"
- 写 SQL 过滤时,这个对应关系由数据本身保证(`update_basic` 从 tushare 拉的就是合法的)
- `industry` 字段是 15 列之一,但 **`get_basic` 不支持 industry 过滤**(代码只有 exchange/market/list_status 三个过滤参数)。需要按行业过滤时自己写 SQL 或在 pandas 里 `df[df.industry == '银行']`

**写入规则**:
- `update_basic(bFull=False)` 调 tushare 拉 L + P 两个状态,concat + 去重 + upsert
- `bFull=True` 时 `DELETE FROM tbl_cn_basic` 后整体 upsert(谨慎使用,会清掉 P 状态的本地更新)
- 断点:`tbl_basic_ctrl.cn_basic = snap_ts()`(snap_ts 不是日期,只是"这次拉过"的标记,**因为 `tbl_cn_basic` 是全量表,无日期维度,所以"上次拉过"用时间戳记录,不是用日期推进**)
- **历史差异**:`cn_basic` 用 ISO datetime,其他 7 个 key 都用 YYYYMMDD。原因是 basic 是一次性全量拉取,不分日;其他表按日期循环,有自然日期维度

---

### 2.2 `tbl_cn_tradecal` — 交易日历

| 项 | 值 |
|---|---|
| 主键 | `(cal_date, exchange)` 复合主键 |
| 写入者 | `CNDataDown.update_tradecal()` |
| 读取者 | `get_tradecal()` |
| 数据源 | tushare `pro.trade_cal` |
| 数据行数 | 6,358(2018-01-01 起,3 个交易所) |
| 起始日期 | 20180101 |

**字段(5)**:

| 字段 | 类型 | 语义 |
|---|---|---|
| `cal_date` | TEXT PK | 日历日 YYYYMMDD |
| `exchange` | TEXT PK | `SSE` / `SZSE` / `BSE` |
| `is_open` | INTEGER | 1=开市 / 0=休市 |
| `pretrade_date` | TEXT | 上一交易日 YYYYMMDD(开市日才有) |
| `snap_ts` | TEXT | 写入时间 |

**索引**:
- PK: `(cal_date, exchange)`
- `idx_tradecal_date` ON `(cal_date)`(只按日期查时用)

**写入规则**:
- 按 SSE / SZSE / BSE 三个交易所**分别循环拉**(每个交易所独立断点 `cn_tradecal_SSE` / `cn_tradecal_SZSE`)
- 增量:从 `ctrl[cn_tradecal_<exch>]` 的 max_date + 1 开始
- 全量:从 20180101 开始
- BSE 实际从未拉到数据(DB 里只有 SSE + SZSE),2026-09-15 修复版未清理历史空行

---

### 2.2.5 `tbl_cn_stk_limit` — 每日涨跌停价格(2026-09-15 新增)

| 项 | 值 |
|---|---|
| 主键 | `(trade_date, ts_code)` 复合主键 |
| 写入者 | `CNDataDown.update_stk_limit()` |
| 读取者 | `get_stk_limit()` |
| 数据源 | tushare `pro.stk_limit`(文档 https://tushare.pro/document/2?doc_id=183) |
| 限制 | 单次最多 5800 条(A 股全市场 ~5438 条/日,不用分页) |
| 起始日期 | 默认 20150101 |

**字段(5)**:

| 字段 | 类型 | 语义 |
|---|---|---|
| `trade_date` | TEXT PK | 交易日 YYYYMMDD |
| `ts_code` | TEXT PK | 股票 / 场内基金代码 |
| `up_limit` | REAL | 涨停价 |
| `down_limit` | REAL | 跌停价 |
| `snap_ts` | TEXT | 写入时间 |

**索引**:
- PK: `(trade_date, ts_code)`
- `idx_stk_limit_date` ON `(trade_date)`(按日期查全市场用)

**写入规则**:
- 按日循环(实测 tushare `start_date + end_date` 范围参数不生效,只能 `trade_date` 单日拉)
- 单日全市场 ~5438 行,无分页
- 断点:`tbl_basic_ctrl.cn_stk_limit`
- 空日不推进断点(节假日)

**特殊说明**:
- tushare `pro.stk_limit` 的 `start_date` / `end_date` 范围参数**实测不生效**,只能 `trade_date` 单日拉
- 包含 A 股 + 场内基金(ETF/LOF),**不区分**,都存进同一张表

**性能**(实测 2026-09-15):
- 单日拉取 0.2s,写 5438 行
- 全量 2 年(503 个交易日):**3.5 分钟**,**+262 万行**
- 限流偶尔触发(Tushare 200/分钟),`RateLimiter` 自动 sleep 17s

---

### 2.2.6 `tbl_cn_suspend` — 每日停复牌信息(2026-09-15 新增)

| 项 | 值 |
|---|---|
| 主键 | `(trade_date, ts_code)` 复合主键 |
| 写入者 | `CNDataDown.update_suspend()` |
| 读取者 | `get_suspend()` |
| 数据源 | tushare `pro.suspend_d`(文档 https://tushare.pro/document/2?doc_id=214) |
| 限制 | 单次最多 5000 条;积分要求 2000+ |
| 起始日期 | 实际 **2015-01-05**(接口从 tushare 早期就有) |
| DB | `db_cn_basic.db`(2026-09-15 加,跟 stk_limit 同库) |

**字段(5)**:

| 字段 | 类型 | 语义 |
|---|---|---|
| `trade_date` | TEXT PK | YYYYMMDD |
| `ts_code` | TEXT PK | 股票代码 |
| `suspend_timing` | TEXT | 日内停牌时间段(如 `09:30-10:00`),日内停牌才有值,否则 NULL |
| `suspend_type` | TEXT PK | S=停牌 / R=复牌 |
| `snap_ts` | TEXT | 写入时间 |

**索引**:
- PK: `(trade_date, ts_code)`
- `idx_suspend_date` ON `(trade_date)`
- `idx_suspend_code` ON `(ts_code)`

**写入规则**:
- **按月循环**(实测 `start_date+end_date` 范围参数有效)
- 单月行数 ~200,远低于 5000 限制,不用分页
- 503 个月 ≈ 503 次调用,**不会触限流**,~5 分钟跑完
- 断点:`tbl_basic_ctrl.cn_suspend`

**特殊说明**:
- **停牌期间每天都有一行快照**(覆盖从停牌第一天到复牌前一天)
- `suspend_timing` 仅日内停牌有值,NULL 表示全天停牌

**性能**(实测 2026-09-15):
- 全量 11.7 年:**281,758 行,~30 秒,503 次调用**
- S(停牌)271,096 行(96.2%)+ R(复牌)10,662 行(3.8%)
- 唯一 ts_code 4,401 只

---

### 2.2.7 `tbl_cn_top_list` — 龙虎榜每日活跃(2026-09-15 新增)

| 项 | 值 |
|---|---|
| 主键 | `(trade_date, ts_code)` 复合主键 |
| 写入者 | `CNDataDown.update_top_list()` |
| 读取者 | `get_top_list()` |
| 数据源 | tushare `pro.top_list`(文档 https://tushare.pro/document/2?doc_id=106) |
| 限制 | **必填 trade_date(单日),不支持范围**;积分要求 2000+ |
| 起始日期 | 实际 **2015-01-05**(接口早期就有) |
| DB | `db_cn_kpl.db`(2026-09-15 新增,跟 kpl_list 同库) |

**字段(16)**:

| 字段 | 类型 | 语义 |
|---|---|---|
| `trade_date` | TEXT PK | YYYYMMDD |
| `ts_code` | TEXT PK | 股票代码 |
| `name` | TEXT | 名称 |
| `close` | REAL | 收盘价 |
| `pct_change` | REAL | 涨跌幅 |
| `turnover_rate` | REAL | 换手率 |
| `amount` | REAL | 总成交额 |
| `l_sell` | REAL | 卖方营业部总金额 |
| `l_buy` | REAL | 买方营业部总金额 |
| `l_amount` | REAL | 买卖总金额 |
| `net_amount` | REAL | 净额 |
| `net_rate` | REAL | 净买额占比 |
| `amount_rate` | REAL | 成交额占比 |
| `float_values` | REAL | 流通市值 |
| `reason` | TEXT | 上榜原因 |
| `snap_ts` | TEXT | 写入时间 |

**索引**:
- PK: `(trade_date, ts_code)`
- `idx_top_list_date` ON `(trade_date)`

**写入规则**:
- **按日循环**(实测无范围参数支持,只能单日拉)
- 单日 ~65 行,无分页
- 断点:`tbl_kpl_ctrl.cn_top_list`
- 跳过周六周日(周末不调)

**特殊说明**:
- 上榜原因示例:`日跌幅偏离值达到7%的前5只证券`、`日换手率达到20%的前5只证券` 等
- `l_sell` + `l_buy` = `l_amount`(买卖总金额)

**性能**(实测 2026-09-15):
- 全量 11.7 年:103,941 行,~7 分钟,1404 次调用
- 平均 65 行/日

---

### 2.2.8 `tbl_cn_top_inst` — 龙虎榜机构买卖明细(2026-09-15 新增)

| 项 | 值 |
|---|---|
| 主键 | `(trade_date, ts_code, exalter, side)` 复合主键 |
| 写入者 | `CNDataDown.update_top_inst()` |
| 读取者 | `get_top_inst()` |
| 数据源 | tushare `pro.top_inst`(文档 https://tushare.pro/document/2?doc_id=107) |
| 限制 | **必填 trade_date(单日),不支持范围**;积分要求 2000+ |
| 起始日期 | 实际 **2015-01-05** |
| DB | `db_cn_kpl.db` |

**字段(11)**:

| 字段 | 类型 | 语义 |
|---|---|---|
| `trade_date` | TEXT PK | YYYYMMDD |
| `ts_code` | TEXT PK | 股票代码 |
| `exalter` | TEXT PK | 营业部 / 机构名称(如"东方财富证券股份有限公司拉萨团结路第一证券营业部") |
| `buy` | REAL | 买入金额 |
| `buy_rate` | REAL | 买入金额占比(%) |
| `sell` | REAL | 卖出金额 |
| `sell_rate` | REAL | 卖出金额占比(%) |
| `net_buy` | REAL | 净额(买入 - 卖出) |
| `side` | TEXT PK | 买卖方向(0=买,1=卖 或类似) |
| `reason` | TEXT | 上榜原因 |
| `snap_ts` | TEXT | 写入时间 |

**索引**:
- PK: `(trade_date, ts_code, exalter, side)`
- `idx_top_inst_date` ON `(trade_date)`
- `idx_top_inst_code` ON `(ts_code)`

**写入规则**:
- **按日循环**(实测无范围参数)
- 单日 ~533 行,**数据量大**
- 断点:`tbl_kpl_ctrl.cn_top_inst`

**性能**(实测 2026-09-15):
- 全量 11.7 年:估算 ~300 万行,~30 分钟,1400 次调用
- 平均 533 行/日

---

### 2.2.9 `tbl_cn_block_trade` — 大宗交易(2026-09-15 新增)

| 项 | 值 |
|---|---|
| 主键 | `(trade_date, ts_code, buyer, seller)` 复合主键 |
| 写入者 | `CNDataDown.update_block_trade()` |
| 读取者 | `get_block_trade()` |
| 数据源 | tushare `pro.block_trade`(文档 https://tushare.pro/document/2?doc_id=355) |
| 限制 | 单次最多 1000 条;积分要求 2000+ |
| 实际起始 | **2020-12-29**(实测) |
| DB | `db_cn_kpl.db`(2026-09-15 加) |

**字段(8)**:

| 字段 | 类型 | 语义 |
|---|---|---|
| `trade_date` | TEXT PK | YYYYMMDD |
| `ts_code` | TEXT PK | 股票代码 |
| `price` | REAL | 成交价 |
| `vol` | REAL | 成交量(万股) |
| `amount` | REAL | 成交金额(万元) |
| `buyer` | TEXT PK | 买方营业部 |
| `seller` | TEXT PK | 卖方营业部 |
| `snap_ts` | TEXT | 写入时间 |

**索引**:
- PK: `(trade_date, ts_code, buyer, seller)`
- `idx_block_trade_date` ON `(trade_date)`
- `idx_block_trade_code` ON `(ts_code)`

**写入规则**:
- **按月循环 + OFFSET 分页**(单次最多 1000 行,可能分页)
- 起始 ~ 今天 = ~70 个月
- 断点:`tbl_kpl_ctrl.cn_block_trade`

---

### 2.2.10 `tbl_cn_ggt_daily` — 港股通每日成交(2026-09-15 新增)

| 项 | 值 |
|---|---|
| 主键 | `trade_date` 单 PK |
| 写入者 | `CNDataDown.update_ggt_daily()` |
| 读取者 | `get_ggt_daily()` |
| 数据源 | tushare `pro.ggt_daily`(文档 https://tushare.pro/document/2?doc_id=298) |
| 限制 | 单次最多 1000 条;积分要求 2000+ |
| 实际起始 | **2015-01-05**(实测) |
| DB | `db_cn_kpl.db` |

**字段(6)**:

| 字段 | 类型 | 语义 |
|---|---|---|
| `trade_date` | TEXT PK | YYYYMMDD |
| `buy_amount` | REAL | 买入成交金额(亿元) |
| `buy_volume` | REAL | 买入成交笔数(万笔) |
| `sell_amount` | REAL | 卖出成交金额(亿元) |
| `sell_volume` | REAL | 卖出成交笔数(万笔) |
| `snap_ts` | TEXT | 写入时间 |

**索引**:
- PK: `trade_date`
- `idx_ggt_date` ON `(trade_date)` — 冗余索引(PK 已自动建索引,保留为兼容性)

**写入规则**:
- **按月循环**(实测范围参数有效)
- 单月 ~30 行,远低于 1000 限制
- 断点:`tbl_kpl_ctrl.cn_ggt_daily`

---

### 2.2.11 `tbl_cn_hsgt_top10` — 沪深股通十大成交股(2026-09-15 新增)

| 项 | 值 |
|---|---|
| 主键 | `(trade_date, ts_code, market_type)` 复合主键 |
| 写入者 | `CNDataDown.update_hsgt_top10()` |
| 读取者 | `get_hsgt_top10()` |
| 数据源 | tushare `pro.hsgt_top10`(文档 https://tushare.pro/document/2?doc_id=356) |
| 限制 | 积分要求 2000+ |
| 实际起始 | **2014-11-17**(沪港通开通日,但实测 tushare 早期也有数据) |
| DB | `db_cn_kpl.db` |

**字段(12)**:

| 字段 | 类型 | 语义 |
|---|---|---|
| `trade_date` | TEXT PK | YYYYMMDD |
| `ts_code` | TEXT PK | 股票代码 |
| `name` | TEXT | 名称 |
| `close` | REAL | 收盘价 |
| `change` | REAL | 涨跌幅 |
| `rank` | INTEGER | 排名(1-10) |
| `market_type` | TEXT PK | 市场类型:`1`=沪(SH),`3`=深(SZ) |
| `amount` | REAL | 成交金额 |
| `net_amount` | REAL | 净成交金额 |
| `buy` | REAL | 买入金额 |
| `sell` | REAL | 卖出金额 |
| `snap_ts` | TEXT | 写入时间 |

**索引**:
- PK: `(trade_date, ts_code, market_type)`
- `idx_hsgt_date` ON `(trade_date)`
- `idx_hsgt_code` ON `(ts_code)`

**写入规则**:
- **按月循环**(实测范围参数有效,单月 ~600 行)
- 起始 ~ 今天 = ~143 个月
- 断点:`tbl_kpl_ctrl.cn_hsgt_top10`

**注意**:
- `market_type` 是数字 `1`(沪) / `3`(深),**不是** SH/SZ 字符串
- 查询要用 `market_type='1'` 或 `market_type='3'` 过滤

---

### 2.2.12 `tbl_cn_limit_list` — 每日涨跌停列表(2026-09-15 新增)

| 项 | 值 |
|---|---|
| 主键 | `(trade_date, ts_code, "limit")` 复合主键(`limit` 是 SQLite 保留字,需引号) |
| 写入者 | `CNDataDown.update_limit_list()` |
| 读取者 | `get_limit_list()` |
| 数据源 | tushare `pro.limit_list_d`(文档 https://tushare.pro/document/2?doc_id=298) |
| 限制 | 单次最多 2500 条;积分要求 5000+(200次/分钟,1万次/天) |
| 实际起始 | **2019-11-28**(用户告知,实测 2019-11-28 有 26 行涨停) |
| DB | `db_cn_kpl.db` |

**字段(19,含 snap_ts)**:

| 字段 | 类型 | 语义 |
|---|---|---|
| `trade_date` | TEXT PK | YYYYMMDD |
| `ts_code` | TEXT PK | 股票代码 |
| `industry` | TEXT | 所属行业 |
| `name` | TEXT | 股票名称 |
| `close` | REAL | 收盘价 |
| `pct_chg` | REAL | 涨跌幅 |
| `amount` | REAL | 成交额 |
| `limit_amount` | REAL | 板上成交金额(跌停价总和,涨停无) |
| `float_mv` | REAL | 流通市值 |
| `total_mv` | REAL | 总市值 |
| `turnover_ratio` | REAL | 换手率 |
| `fd_amount` | REAL | 封单金额 |
| `first_time` | TEXT | 首次封板时间 |
| `last_time` | TEXT | 最后封板时间 |
| `open_times` | INTEGER | 炸板次数(跌停为开板次数) |
| `up_stat` | TEXT | 涨停统计(N/T) |
| `limit_times` | INTEGER | 连板数 |
| `"limit"` | TEXT PK | **D=跌停 / U=涨停 / Z=炸板**(SQLite 保留字) |
| `snap_ts` | TEXT | 写入时间 |

**索引**:
- PK: `(trade_date, ts_code, "limit")`
- `idx_limit_list_date` ON `(trade_date)`
- `idx_limit_list_code` ON `(ts_code)`
- `idx_limit_list_type` ON `("limit")`

**写入规则**:
- **按月循环**,**3 个 limit_type(U/D/Z)各跑一次**
- 全量 ~70 个月 × 3 = 210 次调用,实测 ~5 分钟
- 断点:`tbl_kpl_ctrl.cn_limit_list`

**注意**:
- `limit` 是 SQLite 保留字,SQL 需加反引号
- tushare 接口文档 doc_id=298(原以为是 ggt_daily,实际是 limit_list_d)

**性能**(实测 2026-09-15):
- 单月 2024-09:2,231 行,~0.4s
- 全量 2019-11-28 ~ 2026-09-15(7 年):**165,451 行,~25 秒,357 次调用**
- 涨停 U 102,169 行 + 跌停 D 25,653 行 + 炸板 Z 37,629 行

---

### 2.2.13 `tbl_cn_moneyflow` — 个股资金流向(2026-09-15 新增)

| 项 | 值 |
|---|---|
| 主键 | `(trade_date, ts_code)` 复合主键 |
| 写入者 | `CNDataDown.update_moneyflow()` |
| 读取者 | `get_moneyflow()` |
| 数据源 | tushare `pro.moneyflow`(文档 https://tushare.pro/document/2?doc_id=170) |
| 限制 | 单次最多 6000 条;积分要求 2000+ |
| 实际起始 | **2015-01-05**(文档说 2010 起,实测 2015 起) |
| DB | `db_cn_basic.db` |

**字段(21,含 snap_ts)**:

| 字段 | 类型 | 语义 |
|---|---|---|
| `trade_date` | TEXT PK | YYYYMMDD |
| `ts_code` | TEXT PK | 股票代码 |
| `buy_sm_vol` | INTEGER | 小单(<5万)买入量(手) |
| `buy_sm_amount` | REAL | 小单买入金额(万元) |
| `sell_sm_vol` | INTEGER | 小单卖出量 |
| `sell_sm_amount` | REAL | 小单卖出金额 |
| `buy_md_vol` | INTEGER | 中单(5万~20万)买入量 |
| `buy_md_amount` | REAL | 中单买入金额 |
| `sell_md_vol` | INTEGER | 中单卖出量 |
| `sell_md_amount` | REAL | 中单卖出金额 |
| `buy_lg_vol` | INTEGER | 大单(20万~100万)买入量 |
| `buy_lg_amount` | REAL | 大单买入金额 |
| `sell_lg_vol` | INTEGER | 大单卖出量 |
| `sell_lg_amount` | REAL | 大单卖出金额 |
| `buy_elg_vol` | INTEGER | 特大单(≥100万)买入量 |
| `buy_elg_amount` | REAL | 特大单买入金额 |
| `sell_elg_vol` | INTEGER | 特大单卖出量 |
| `sell_elg_amount` | REAL | 特大单卖出金额 |
| `net_mf_vol` | INTEGER | 净流入量(手) |
| `net_mf_amount` | REAL | 净流入额(万元) |
| `snap_ts` | TEXT | 写入时间 |

**索引**:
- PK: `(trade_date, ts_code)`
- `idx_moneyflow_date` ON `(trade_date)`
- `idx_moneyflow_code` ON `(ts_code)`

**写入规则**:
- **按月循环 + OFFSET 分页**(单月 > 6000,需分页)
- 全量 141 个月 × 1-2 页 = ~200 次调用,限流 200/分钟,约 1-2 分钟跑完
- 断点:`tbl_basic_ctrl.cn_moneyflow`

**注意**:
- 单月行数 > 6000(实测),需要 OFFSET 分页
- 净流入为主动买卖单的净值,不是大小单总和相减

**性能**(实测 2026-09-15):
- 单月 2024-09:96,676 行,12.8s
- 单日 2024-09-13:5088 行(< 6000 限制)

---

### 2.2.14 `tbl_cn_margin` — 融资融券交易汇总(2026-09-15 新增)

| 项 | 值 |
|---|---|
| 主键 | `(trade_date, exchange_id)` 复合主键 |
| 写入者 | `CNDataDown.update_margin()` |
| 读取者 | `get_margin()` |
| 数据源 | tushare `pro.margin`(文档 https://tushare.pro/document/2?doc_id=58) |
| 限制 | 单次最多 3 行(SSE + SZSE + BSE),无 range 参数支持 |
| 实际起始 | **2015-01-05**(用户要求从 2015-01-01 开始,实测 2015-01-05 最早有数据;BSE 2021 开业前只有 SSE/SZSE 2 行) |
| DB | `db_cn_basic.db` |

**字段(10,含 snap_ts)**:

| 字段 | 类型 | 语义 |
|---|---|---|
| `trade_date` | TEXT PK | YYYYMMDD |
| `exchange_id` | TEXT PK | SSE / SZSE / BSE |
| `rzye` | REAL | 融资余额(元) |
| `rzmre` | REAL | 融资买入额(元) |
| `rzche` | REAL | 融资偿还额(元) |
| `rqye` | REAL | 融券余额(元) |
| `rqmcl` | REAL | 融券卖出量(股) |
| `rzrqye` | REAL | 融资融券余额(元) |
| `rqyl` | REAL | 融券余量(股) |
| `snap_ts` | TEXT | 写入时间 |

**索引**:
- PK: `(trade_date, exchange_id)`
- `idx_margin_date` ON `(trade_date)`

**写入规则**:
- **按日循环**(无 range 参数,必须单日)
- 跳过周末(融资融券数据仅交易日)
- 数据量小,15 年 × 250 天 = ~3750 天 ≈ 1 万行
- 断点:`tbl_basic_ctrl.cn_margin`

**调度位置**:**`task_morning_update`**(09:05 跑,用户指定)— 融资融券数据盘后才出,早上抓"昨天"

**性能**(实测 2026-09-15):
- 单日 3 行,0.4s,1 次调用

---

### 2.2.15 `tbl_cn_margin_detail` — 融资融券交易明细(2026-09-15 新增)

| 项 | 值 |
|---|---|
| 主键 | `(trade_date, ts_code)` 复合主键 |
| 写入者 | `CNDataDown.update_margin_detail()` |
| 读取者 | `get_margin_detail()` |
| 数据源 | tushare `pro.margin_detail`(文档 https://tushare.pro/document/2?doc_id=59) |
| 限制 | 单次最多 6000 条,单月通常 2000-6000 行(2018 早期 ~1000,2024 ~4000),需 OFFSET 分页 |
| 实际起始 | **2014-12-31**(用户要求从 2015-01-01 开始,实测 2014-12-31 最早有数据;文档说 2019-09-10 起,实测更早) |
| DB | `db_cn_basic.db` |

**字段(11,含 snap_ts)**:

| 字段 | 类型 | 语义 |
|---|---|---|
| `trade_date` | TEXT PK | YYYYMMDD |
| `ts_code` | TEXT PK | 股票代码 |
| `rzye` | REAL | 融资余额(元) |
| `rqye` | REAL | 融券余额(元) |
| `rzmre` | REAL | 融资买入额(元) |
| `rqyl` | REAL | 融券余量(股) |
| `rzche` | REAL | 融资偿还额(元) |
| `rqchl` | REAL | 融券偿还量(股) |
| `rqmcl` | REAL | 融券卖出量(股) |
| `rzrqye` | REAL | 融资融券余额(元) |
| `snap_ts` | TEXT | 写入时间 |

**索引**:
- PK: `(trade_date, ts_code)`
- `idx_margin_detail_date` ON `(trade_date)`
- `idx_margin_detail_code` ON `(ts_code)`

**写入规则**:
- **按月循环 + OFFSET 分页**(单月 2000-6000 行,需 OFFSET 0-1 次)
- 断点:`tbl_basic_ctrl.cn_margin_detail`
- 全量 2014-12 ~ 2026-09 = ~141 个月,约 200-250 次调用

**调度位置**:**`task_morning_update`**(09:05 跑,跟 `service_margin` 一起)— 融资融券数据盘后才出

**性能**(实测 2026-09-15):
- 单日 3923 行,0.5s,1 次调用

---

### 2.2.16 `tbl_cn_cyq_perf` — 每日筹码及胜率(2026-09-15 新增)

| 项 | 值 |
|---|---|
| 主键 | `(trade_date, ts_code)` 复合主键 |
| 写入者 | `CNDataDown.update_cyq_perf()` |
| 读取者 | `get_cyq_perf()` |
| 数据源 | tushare `pro.cyq_perf`(文档 https://tushare.pro/document/2?doc_id=293) |
| 限制 | 单次最多 6000 条;积分要求 5000+;接口要求 `ts_code` 或 `trade_date` 至少传 1 个 |
| 实际起始 | **2020-01-02**(用户要求 2020-01-01 起,实测最早有数据;文档说 2018 起) |
| DB | `db_cn_basic.db` |

**字段(12,含 snap_ts)**:

| 字段 | 类型 | 语义 |
|---|---|---|
| `trade_date` | TEXT PK | YYYYMMDD |
| `ts_code` | TEXT PK | 股票代码 |
| `his_low` | REAL | 历史最低价 |
| `his_high` | REAL | 历史最高价 |
| `cost_5pct` | REAL | 5% 获利盘成本 |
| `cost_15pct` | REAL | 15% 获利盘成本 |
| `cost_50pct` | REAL | 50% 获利盘成本(中位成本) |
| `cost_85pct` | REAL | 85% 获利盘成本 |
| `cost_95pct` | REAL | 95% 获利盘成本 |
| `weight_avg` | REAL | 加权平均成本 |
| `winner_rate` | REAL | 胜率(%) |
| `snap_ts` | TEXT | 写入时间 |

**索引**:
- PK: `(trade_date, ts_code)`
- `idx_cyq_perf_date` ON `(trade_date)`
- `idx_cyq_perf_code` ON `(ts_code)`
- `idx_cyq_perf_winner` ON `(winner_rate)` — 加速按胜率过滤

**写入规则**:
- **按日循环**(单日全市场 ~5578 行,接口要求 `trade_date` 必传才能不传 ts_code)
- 数据 18-21 点更新
- 断点:`tbl_basic_ctrl.cn_cyq_perf`
- 全量 2020-01 ~ 2026-09 = ~1660 天,约 1660 次调用

**调度位置**:**`task_full_update` 阶段 4**(每日 18:00 后跑,数据 18-21 点更新)

**性能**(实测 2026-09-15):
- 单日 5578 行,0.5s,1 次调用

---

### 2.2.17 `tbl_cn_daily_basic` — 每日指标(2026-09-15 新增)

| 项 | 值 |
|---|---|
| 主键 | `(trade_date, ts_code)` 复合主键 |
| 写入者 | `CNDataDown.update_daily_basic()` |
| 读取者 | `get_daily_basic()` |
| 数据源 | tushare `pro.daily_basic`(文档 https://tushare.pro/document/2?doc_id=32) |
| 限制 | 单次最多 6000 条;积分要求 2000+;接口推荐传 `limit` + `status` |
| 实际起始 | **2015-01-05**(用户要求 2015-01-01 起,实测最早) |
| DB | `db_cn_basic.db` |
| 数据行数 | 11,646,614 |

**字段(19,含 snap_ts)**:

| 字段 | 类型 | 语义 |
|---|---|---|
| `trade_date` | TEXT PK | YYYYMMDD |
| `ts_code` | TEXT PK | 股票代码 |
| `close` | REAL | 收盘价 |
| `turnover_rate` | REAL | 换手率(%) |
| `turnover_rate_f` | REAL | 换手率(自由流通股) |
| `volume_ratio` | REAL | 量比 |
| `pe` | REAL | 市盈率(动) |
| `pe_ttm` | REAL | 市盈率 TTM |
| `pb` | REAL | 市净率 |
| `ps` | REAL | 市销率 |
| `ps_ttm` | REAL | 市销率 TTM |
| `dv_ratio` | REAL | 股息率(%) |
| `dv_ttm` | REAL | 股息率 TTM |
| `total_share` | REAL | 总股本(万股) |
| `float_share` | REAL | 流通股本(万股) |
| `free_share` | REAL | 自由流通股本(万股) |
| `total_mv` | REAL | 总市值(万元) |
| `circ_mv` | REAL | 流通市值(万元) |
| `snap_ts` | TEXT | 写入时间 |

**索引**:
- PK: `(trade_date, ts_code)`
- `idx_daily_basic_date` ON `(trade_date)`
- `idx_daily_basic_code` ON `(ts_code)`

**写入规则**:
- **按日循环**(单日全市场 ~5341 行 status='L',< 6000 限制)
- 显式传 `limit=6000, status='L'`(用户提示文档说必填)
- 数据每日 15-17 点更新
- 断点:`tbl_basic_ctrl.cn_daily_basic`
- 全量 2015-01-05 ~ 2026-09-15 = ~2850 天,~2850 次调用,触发限流 sleep 22s 共 ~70 分钟

**调度位置**:**`task_full_update` 阶段 4**(每日 18:00 后跑,数据 15-17 点更新)

**性能**(实测 2026-09-15):
- 单日 5341 行,0.4s,1 次调用

---

### 2.3 `tbl_cn_day` — A 股日 K(**不复权原始数据**)

| 项 | 值 |
|---|---|
| 主键 | `(ts_code, trade_date)` 复合主键 |
| 写入者 | `CNDataDown.update_daily()` |
| 读取者 | `get_day()` |
| 数据源 | tushare `pro.daily`(**接口只支持未复权,官方明确**) |
| 数据行数 | 11,725,879(实际每天约 887 只股有数据,5500 只是个保守上限) |
| 起始日期 | 20150101 |

**字段(12)**:

| 字段 | 类型 | 语义 |
|---|---|---|
| `ts_code` | TEXT PK | 股票代码 |
| `trade_date` | TEXT PK | 交易日 YYYYMMDD |
| `open` | REAL | 开盘价(不复权) |
| `high` | REAL | 最高价(不复权) |
| `low` | REAL | 最低价(不复权) |
| `close` | REAL | 收盘价(不复权) |
| `pre_close` | REAL | 昨收 |
| `change` | REAL | 涨跌额 |
| `pct_chg` | REAL | 涨跌幅 % |
| `vol` | REAL | 成交量(手) |
| `amount` | REAL | 成交额(千元) |
| `snap_ts` | TEXT | 写入时间 |

**索引**:
- PK: `(ts_code, trade_date)`
- `idx_day_date` ON `(trade_date)`(按日期查全市场用)

**重要设计:不复权**:
- `tbl_cn_day` 存的是 tushare 返回的原始 OHLCV,**不存前复权**
- 前复权在 `get_day(qfq=True)` 读时 join `tbl_cn_adj_factor` 计算:
  ```
  qfq_factor = adj_factor[当天] / adj_factor[最新]
  open_qfq  = open  * qfq_factor
  close_qfq = close * qfq_factor
  vol_qfq   = vol   / qfq_factor
  amount 不变(成交额不受复权影响)
  ```
- 好处:原始数据可追溯,任意时刻都能根据 adj_factor 重算

**写入规则**:
- 按日期循环(cur=sd..ed)
- 每天 `tushare pro.daily(trade_date=td, limit=6000, offset=offset)` 单日全市场拉
- OFFSET 分页:单日超过 6000 行时分页(2024+ 部分日期可能 > 6000)
- 单日最多 50 页(30 万条)安全上限
- 断点:`tbl_basic_ctrl.cn_daily` = 最后一个**有数据**写入的日期

---

### 2.4 `tbl_cn_adj_factor` — 复权因子

| 项 | 值 |
|---|---|
| 主键 | `(ts_code, trade_date)` 复合主键 |
| 写入者 | `CNDataDown.update_adj_factor()` |
| 读取者 | `get_adj_factor()` |
| 数据源 | tushare `pro.adj_factor` |
| 数据行数 | 12,244,479 |
| 起始日期 | 20150101 |

**字段(4)**:

| 字段 | 类型 | 语义 |
|---|---|---|
| `ts_code` | TEXT PK | 股票代码 |
| `trade_date` | TEXT PK | 交易日 YYYYMMDD |
| `adj_factor` | REAL | 复权因子(浮点,一般 > 1.0 表示历史除权) |
| `snap_ts` | TEXT | 写入时间 |

**索引**:
- PK: `(ts_code, trade_date)`
- `idx_adj_date` ON `(trade_date)`(按日期查全市场用)

**写入规则**:
- 按日期循环,每天 `tushare pro.adj_factor(trade_date=td, limit=6000, offset=offset)`
- OFFSET 分页同 daily
- 断点:`tbl_basic_ctrl.cn_adj_factor` = 最后一个写入日期

**读取时的 qfq 计算**:
- `get_day(qfq=True)` 内部 join `tbl_cn_adj_factor` 取该 ts_code 的**最新** adj_factor 作分母
- 取该 (ts_code, trade_date) 的 adj_factor 作分子
- adj_factor 缺失用 1.0 填充(等价不复权)

---

### 2.5 `tbl_cn_week` — 周 K(**已前复权**,派生表)

| 项 | 值 |
|---|---|
| 主键 | `(ts_code, trade_date)` 复合主键 |
| 写入者 | `CNDataDown.update_week()` |
| 读取者 | `get_week()` |
| 数据源 | **本地派生**(`tbl_cn_day` × `tbl_cn_adj_factor` 聚合) |
| 数据行数 | 2,489,381 |
| 起始日期 | 20150101 |

**字段(12,跟 day 同)**:

| 字段 | 类型 | 语义 |
|---|---|---|
| `ts_code` | TEXT PK | 股票代码 |
| `trade_date` | TEXT PK | 周内最后交易日 YYYYMMDD |
| `open` | REAL | 周内第一日的 open(前复权) |
| `high` | REAL | 周内 high max(前复权) |
| `low` | REAL | 周内 low min(前复权) |
| `close` | REAL | 周内最后一日 close(前复权) |
| `pre_close` | REAL | 上一周 close |
| `change` | REAL | close - pre_close |
| `pct_chg` | REAL | 涨跌幅 % |
| `vol` | REAL | 周内 vol sum |
| `amount` | REAL | 周内 amount sum |
| `snap_ts` | TEXT | 写入时间 |

**索引**:
- PK: `(ts_code, trade_date)`
- `idx_week_date` ON `(trade_date)`

**聚合规则**(ISO 周):
- `open`: 周内第一日 open
- `close`: 周内最后一日 close
- `high`: max(周内 high)
- `low`: min(周内 low)
- `vol`: sum(周内 vol)
- `amount`: sum(周内 amount)
- `trade_date`: 周内最后交易日
- `pre_close`: 上一周 close(shift 1)
- `change`: close - pre_close
- `pct_chg`: (change / pre_close) * 100

**写入规则**:
- **覆盖更新**:`replace_table` 整个 DELETE + INSERT,事务化
- 一次性从 `tbl_cn_day` + `tbl_cn_adj_factor` 算出全部历史周 K
- 启动时 `service_week` 强校验:ctrl.cn_daily == ctrl.cn_adj_factor == today,否则 rc=2 中止

**前置 0 价格过滤**:聚合前过滤 `open>0 & close>0`,避免新股预占位行污染

---

### 2.6 `tbl_cn_month` — 月 K(**已前复权**,派生表)

| 项 | 值 |
|---|---|
| 主键 | `(ts_code, trade_date)` 复合主键 |
| 写入者 | `CNDataDown.update_month()` |
| 读取者 | `get_month()` |
| 数据源 | **本地派生**(`tbl_cn_day` × `tbl_cn_adj_factor` 聚合) |
| 数据行数 | 593,382 |

**字段(12,跟 week 同)**:但 trade_date 是月内最后交易日

**索引**:
- PK: `(ts_code, trade_date)`
- `idx_month_date` ON `(trade_date)`

**聚合规则**(自然月,YYYYMM 分组):
- 同 week,但 `_group = trade_date[:6]`(YYYYMM)

**写入规则**:同 week,覆盖更新 + 强校验

---

### 2.7 `tbl_basic_ctrl` — 基础库断点表(2026-09-15 重构为"每个 DB 一个 ctrl 表")

| 项 | 值 |
|---|---|
| 主键 | `key` |
| 写入者 | `update_ctrl(conn, key, max_date)`(多个 update_* 共享) |
| 读取者 | `get_ctrl_basic()` / `get_ctrl(conn, key)`(内部 helper) |
| 数据行数 | 12 |

**字段(3)**:

| 字段 | 类型 | 语义 |
|---|---|---|
| `key` | TEXT PK | 断点 key,见下表 |
| `max_date` | TEXT | 已拉到的最大日期(YYYYMMDD)或时间戳(basic 的是 ISO datetime) |
| `updated_at` | TEXT | 上次更新时间 ISO |

**12 个 key**(只管 basic DB 内的数据表;kpl 表 ctrl 在 `tbl_kpl_ctrl`,index 表 ctrl 在 `tbl_index_ctrl`,news ctrl 在 `tbl_news_ctrl`):
| key | 表 | 类型 |
|---|---|---|
| `cn_basic` | tbl_cn_basic | snap_ts(无日期维度) |
| `cn_daily` | tbl_cn_day | YYYYMMDD |
| `cn_adj_factor` | tbl_cn_adj_factor | YYYYMMDD |
| `cn_tradecal_SSE` | tbl_cn_tradecal | YYYYMMDD(每个交易所独立) |
| `cn_tradecal_SZSE` | tbl_cn_tradecal | YYYYMMDD |
| `cn_stk_limit` | tbl_cn_stk_limit | YYYYMMDD(每日涨跌停价格,2026-09-15 新增) |
| `cn_suspend` | tbl_cn_suspend | YYYYMMDD(每日停复牌,2026-09-15 新增) |
| `cn_moneyflow` | tbl_cn_moneyflow | YYYYMMDD(个股资金流向,2026-09-15 新增) |
| `cn_margin` | tbl_cn_margin | YYYYMMDD(融资融券交易汇总,2026-09-15 新增) |
| `cn_margin_detail` | tbl_cn_margin_detail | YYYYMMDD(融资融券交易明细,2026-09-15 新增) |
| `cn_cyq_perf` | tbl_cn_cyq_perf | YYYYMMDD(每日筹码及胜率,2026-09-15 新增) |
| `cn_daily_basic` | tbl_cn_daily_basic | YYYYMMDD(每日指标,2026-09-15 新增) |

**设计原则**(2026-09-15 重构):**每个数据库都有自己的 ctrl 表,本地管理**。
- `db_cn_basic.db` → `tbl_basic_ctrl`(12 keys,管 basic DB 表)
- `db_cn_kpl.db` → `tbl_kpl_ctrl`(9 keys,管 kpl DB 表,2026-09-15 重建)
- `db_cn_index.db` → `tbl_index_ctrl`(2 keys,2026-09-15 新建)
- `db_cn_news.db` → `tbl_news_ctrl`(11 src,管新闻源)

**历史**:`tbl_basic_ctrl` 曾跨域管理所有 ctrl(包括 kpl 表、index 表)。2026-09-15 按"每个 DB 一个 ctrl"原则重构:
- kpl 表 ctrl → 迁到 `tbl_kpl_ctrl`(in kpl DB)
- index 表 ctrl → 迁到 `tbl_index_ctrl`(in index DB)
- news ctrl 本来就在 kpl DB,保留不动

---

## 3. db_cn_kpl.db (1.1 GB, 3 表)

### 3.1 `tbl_cn_kpl_list` — 开盘啦涨停榜(25 列,2026-09-15 加炸板)

| 项 | 值 |
|---|---|
| 主键 | `(ts_code, trade_date, tag)` 复合主键(2026-09-15 加 tag,区分涨停 vs 炸板) |
| 写入者 | `CNDataDown.update_kpl_list(tag=...)` |
| 读取者 | `get_kpl_list(tags=...)` |
| **数据源平台** | **tushare(pro.tushare.pro 平台)** |
| **API 客户端** | `coreClient.tushare_client.TushareClient` |
| **接口 endpoint** | `pro.kpl_list`(支持 `tag` 参数:'涨停' / '炸板') |
| 数据源 | **tushare `pro.kpl_list`**(**T+1 滞后,第二天早上才更新**) |
| 起始日期 | 涨停:2018-01-02 起(实测);**炸板:2020-10-09 起**(用户要求) |
| 下载策略 | **涨停**:tushare 默认下载(不传 tag);**炸板**:`service_kpl_list` 默认下载(2020-10-09 起) |

> ⚠️ **数据源是 tushare**(走 `pro_api()` 平台),**不是**同花顺 KPL。
> 同花顺 KPL 接口盘中拉 today 有数据;tushare 接口盘中无数据,第二天早上 8 点才有。

**字段(25)**:

| 字段 | 类型 | 语义 |
|---|---|---|
| `ts_code` | TEXT PK | 股票代码 |
| `name` | TEXT | 简称 |
| `trade_date` | TEXT PK | 涨停日 YYYYMMDD |
| `lu_time` | TEXT | 封板时间 HH:MM:SS |
| `ld_time` | TEXT | 开板时间 |
| `open_time` | TEXT | 开盘时间 |
| `last_time` | TEXT | 最后成交时间 |
| `lu_desc` | TEXT | 涨停原因简称,如 `印制电路板` |
| `tag` | TEXT PK | **`涨停` / `炸板`**(2026-09-15 改造:同一只股票同一天 tag 唯一,要么以涨停收盘,要么盘中涨停过但收盘未封住) |
| `theme` | TEXT | 题材组合(`、`分隔),如 `军工、低空经济` |
| `net_change` | REAL | 净额 |
| `bid_amount` | REAL | 封单金额 |
| `status` | TEXT | 连板标记:`首板` / `2连板` / `3天2板` 等 |
| `bid_change` | REAL | 封单变化 |
| `bid_turnover` | REAL | 封单换手 |
| `lu_bid_vol` | REAL | 封单量 |
| `pct_chg` | REAL | 涨幅 % |
| `bid_pct_chg` | REAL | 封单涨幅 |
| `rt_pct_chg` | REAL | 实时涨幅 |
| `limit_order` | REAL | 涨停封单(元) |
| `amount` | REAL | 成交额 |
| `turnover_rate` | REAL | 换手率 % |
| `free_float` | REAL | 流通市值 |
| `lu_limit_order` | REAL | 涨停板封单 |
| `snap_ts` | TEXT | 写入时间 |

**索引**:
- PK: `(ts_code, trade_date, tag)` — 2026-09-15 加 tag(支持涨停和炸板独立记录)
- `idx_kpl_date` ON `(trade_date)`(查某日全部涨停)
- `idx_kpl_tag` ON `(tag)`(按 tag 过滤,2026-09-15 新增)

**写入规则**:
- 按日期循环,每天 `tushare pro.kpl_list(trade_date=td, limit=8000, offset=offset)`
- 单日最多 50 页安全上限
- 断点:`tbl_kpl_ctrl.cn_kpl_list`(2026-09-15 改用 kpl DB 本地 ctrl)
- last_success_date 推进规则:只有当日有 INSERT 才推进,空日不推进(2026-09-15 修复版)
- tag 下载顺序:先下载 `tag='炸板'`(不更新 ctrl),再下载 `tag='涨停'`(更新 ctrl)

**特殊字段语义**:
- `status`:`get_kpl_list` 支持关键字展开(非首板 / N板以上 / N天 / 天M板)
- `theme`:逗号串,`get_kpl_list(themes='军工')` 用 `LIKE '%军工%'` 模糊匹配

---

### 3.2 `tbl_cn_kpl_concept_cons` — 开盘啦题材成分(8 列)

| 项 | 值 |
|---|---|
| 主键 | `(ts_code, con_code, trade_date)` 复合主键 |
| 写入者 | `CNDataDown.update_kpl_concept_cons()` |
| 读取者 | `get_kpl_concept_cons()` |
| 数据源 | tushare `pro.kpl_concept` |
| 数据行数 | 4,099,392 |
| 起始日期 | 20240420 |

**字段(8)**:

| 字段 | 类型 | 语义 |
|---|---|---|
| `ts_code` | TEXT PK | **题材代码**(如 `000009.KP`),不是股票代码! |
| `name` | TEXT | **题材名称**(如 `光刻机概念`) |
| `con_name` | TEXT | 成分股简称(如 `上汽集团`) |
| `con_code` | TEXT PK | 成分股代码(如 `600104.SH`) |
| `trade_date` | TEXT PK | 交易日 YYYYMMDD |
| `desc` | TEXT | 题材-公司关系描述 |
| `hot_num` | INTEGER | 题材热度 |
| `snap_ts` | TEXT | 写入时间 |

**索引**:
- PK: `(ts_code, con_code, trade_date)`
- `idx_kplcc_date` ON `(trade_date)`

**重要语义**:
- `ts_code` 是**题材代码**,不是股票代码
- `con_code` 才是成分股代码
- `get_kpl_concept_cons(ts_codes='600104.SH')` 实际查 `con_code`,参数名是历史遗留

---

### 3.3 `tbl_cn_kpl_limit_performance` — 涨停表现详情(24 列,**同花顺实时数据**)

| 项 | 值 |
|---|---|
| 主键 | `(trade_date, ts_code)` 复合主键 |
| 写入者 | `CNDataDown.update_kpl_limit_performance()` |
| 读取者 | `get_kpl_limit_performance()` |
| **数据源平台** | **同花顺(10jqka / iFinD / 长华 aphis.longhuvip.com)** |
| **API 客户端** | `coreClient.kpl_client.KPLClient`(**不是** tushare) |
| **接口 endpoint** | `HisHomeDingPan/DailyLimitPerformance`(历史 host apphis)<br>`HomeDingPan/DailyLimitPerformance`(实时 host apphwhq) |
| **接口策略** | 日期 == today → 实时接口<br>日期 < today → 历史接口 |
| 数据行数 | 12,417 行(2026-01-05 ~ 2026-09-15)|
| 起始日期 | 本周一(默认)+ctrl.max_date 取大 |

**字段(24)**:

| 字段 | 类型 | 语义 |
|---|---|---|
| `trade_date` | TEXT PK | 交易日 YYYYMMDD |
| `ts_code` | TEXT PK | 股票代码(带 .SH/.SZ/.BJ) |
| `name` | TEXT | 简称 |
| `board_type` | INTEGER | PidType 板类型(1-5) |
| `board_count` | INTEGER | 板数(冗余便于查询) |
| `lu_time` | TEXT | 封板时间 HH:MM:SS |
| `theme` | TEXT | 主题材简称 |
| `limit_reason` | TEXT | 涨停原因全文(多题材) |
| `is_break` | INTEGER | 是否曾炸开(0/1) |
| `amplitude` | REAL | 振幅 % |
| `turnover_rate` | REAL | 换手率 % |
| `limit_order` | REAL | 涨停封单金额(元) |
| `lu_limit_order` | REAL | 涨停板封单金额 |
| `net_change` | REAL | 净额(元) |
| `main_in` | REAL | 主力流入(元) |
| `main_out` | REAL | 主力流出(元) |
| `amount` | REAL | 成交额(元) |
| `free_float` | REAL | 流通市值(元) |
| `close_price` | REAL | 收盘价(元) |
| `pct_chg` | REAL | 涨幅 % |
| `board_period` | TEXT | "X天Y板" 板周期描述 |
| `theme_id` | TEXT | 主题材 ID(6 位) |
| `sector_id` | INTEGER | 板块分类 ID |
| `snap_ts` | TEXT | 写入时间 |

**索引**:
- PK: `(trade_date, ts_code)`
- `idx_kpl_lp_date` ON `(trade_date)`
- `idx_kpl_lp_code` ON `(ts_code)`
- `idx_kpl_lp_board` ON `(board_count, lu_time)`(排序用)

**与 `tbl_cn_kpl_list` 的关系**:
- `tbl_cn_kpl_list` 来源 **tushare `pro.kpl_list`**(tushare.pro 平台),**T+1 滞后**(第二天早上更新)
- `tbl_cn_kpl_limit_performance` 来源**同花顺 KPL**(`HisHomeDingPan/DailyLimitPerformance`,aphis.longhuvip.com),**实时(16:30+ 就有,走同花顺实时接口 HomeDingPan)**
- 两表互补,读时按"实时性需求"选表
- 字段差异:本表多 `is_break` / `amplitude` / `close_price` / `board_period` / `board_count` 等细粒度字段

**写入规则**:
- 按日期循环,每天 `KPLClient.get_daily_limit_performance(date)`(date='YYYY-MM-DD' 格式)
- 客户端懒加载:首次调时 `from coreClient.kpl_client import KPLClient`
- 断点:`tbl_kpl_ctrl.cn_kpl_limit_performance`
- 默认起始日期 = `max(本周周一, ctrl.max_date)`(只补本周的)
- last_success_date 推进规则:只有当日有 INSERT 才推进,空日不推进

### 3.4 `tbl_kpl_ctrl` — kpl 库断点表(2026-09-15 重建)

| 项 | 值 |
|---|---|
| 主键 | `key` |
| 写入者 | `update_ctrl_kpl(conn, key, max_date)` |
| 读取者 | `get_ctrl_kpl(key)` |
| 数据行数 | 9 |

**字段(3)**:

| 字段 | 类型 | 语义 |
|---|---|---|
| `key` | TEXT PK | 断点 key,见下表 |
| `max_date` | TEXT | 已拉到的最大日期(YYYYMMDD) |
| `updated_at` | TEXT | 上次更新时间 ISO |

**9 个 key**(管 kpl DB 内的所有数据表):
| key | 表 | 类型 |
|---|---|---|
| `cn_kpl_list` | tbl_cn_kpl_list | YYYYMMDD(2026-09-15 加炸板后,只对涨停更新 ctrl) |
| `cn_kpl_concept_cons` | tbl_cn_kpl_concept_cons | YYYYMMDD |
| `cn_kpl_limit_performance` | tbl_cn_kpl_limit_performance | YYYYMMDD(同花顺数据源,2026-09-15 加) |
| `cn_top_list` | tbl_cn_top_list | YYYYMMDD(龙虎榜每日活跃,2026-09-15 新增) |
| `cn_top_inst` | tbl_cn_top_inst | YYYYMMDD(龙虎榜机构明细,2026-09-15 新增) |
| `cn_block_trade` | tbl_cn_block_trade | YYYYMMDD(大宗交易,2026-09-15 新增) |
| `cn_ggt_daily` | tbl_cn_ggt_daily | YYYYMMDD(港股通日成交,2026-09-15 新增) |
| `cn_hsgt_top10` | tbl_cn_hsgt_top10 | YYYYMMDD(沪深股通十大成交股,2026-09-15 新增) |
| `cn_limit_list` | tbl_cn_limit_list | YYYYMMDD(每日涨跌停列表,2026-09-15 新增) |

**设计原则**(2026-09-15 重建):**本地管理 kpl DB 数据**。2026-09-15 之前曾短暂存在于 kpl DB 中,后被误删;2026-09-15 重建并把 9 个 kpl 表 ctrl key 从 `tbl_basic_ctrl` 迁移过来。

---

## 4. db_cn_news.db (6.4 GB, 4 表)

### 4.1 `tbl_news` — 新闻快讯(9 源聚合,8 列)

| 项 | 值 |
|---|---|
| 主键 | `(datetime, src, md5)` 复合主键 |
| 写入者 | `CNDataDown.update_news()` |
| 读取者 | `get_news()` |
| 数据源 | tushare `pro.news`(9 源) |
| 数据行数 | 9,454,159 |
| 起始日期 | 2020-01-01 |

**字段(8)**:

| 字段 | 类型 | 语义 |
|---|---|---|
| `datetime` | TEXT PK | **YYYY-MM-DD HH:MM:SS**(带时间戳) |
| `src` | TEXT PK | 源标识:`sina` / `wallstreetcn` / `10jqka` / `eastmoney` / `yuncaijing` / `fenghuang` / `jinrongjie` / `cls` / `yicai` |
| `title` | TEXT | 标题 |
| `content` | TEXT | 正文 |
| `channels` | TEXT | 频道 |
| `score` | REAL | 重要性分数 |
| `md5` | TEXT PK | 基于 `content` 算的 md5(主键) |
| `snap_ts` | TEXT | 写入时间 |

**索引**:
- PK: `(datetime, src, md5)`
- `idx_news_date` ON `(datetime)`
- `idx_news_src` ON `(src)`

**md5 计算**:基于 `content` 字段,即使同一时刻同源,内容不同算不同条(同源同刻同内容算同一条)

**9 个源 + 2 个 disabled**:
- 启用:sina, wallstreetcn, 10jqka, eastmoney, jinrongjie, cls, yicai(7 个)
- disabled(2026-09-11 实测 7 天连续 0 行):yuncaijing, fenghuang(2 个)
- `NEWS_SRC_LIST = ['sina', 'wallstreetcn', '10jqka', 'eastmoney', 'yuncaijing', 'fenghuang', 'jinrongjie', 'cls', 'yicai']`
- `NEWS_SRC_DISABLED = {'yuncaijing', 'fenghuang'}`
- `get_active_news_srcs()` 返回 NEWS_SRC_LIST - DISABLED

**写入规则**:
- 9 源(实际 7 源)并行循环
- 每个源独立断点:`tbl_news_ctrl[src]`(YYYY-MM-DD HH:MM:SS 格式)
- 逐日循环:每天 `tushare pro.news(src=src, start_date=td, end_date=td+1, limit=1500, offset=offset)`
- OFFSET 分页:单日最多 100 页(15 万条)安全上限
- md5 在内存中算(content 字段)
- 增量窗口:每源 last 断点的当天 00:00:00 重拉(避免漏当天剩余时间的新料)

**重要:datetime 格式**:`YYYY-MM-DD HH:MM:SS` 带时间戳。`get_news` 查询时:
- `trade_date='2026-09-11'` → 自动 `[2026-09-11 00:00:00, 2026-09-11 23:59:59]`
- `start_datetime='2026-09-11 09:00:00'` → 精确时分秒
- `trade_date` 跟 `*_datetime` 互斥

---

### 4.2 `tbl_major_news` — 长新闻(8 列,跟 news 同结构)

| 项 | 值 |
|---|---|
| 主键 | `(datetime, src, md5)` 复合主键 |
| 写入者 | `CNDataDown.update_major_news()` |
| 读取者 | `get_major_news()` |
| 数据源 | tushare `pro.major_news` |
| 数据行数 | 1,455,343 |

**字段(8)**:跟 `tbl_news` 完全相同(datetime / src / title / content / channels / score / md5 / snap_ts)

**索引**:
- PK: `(datetime, src, md5)`
- `idx_major_date` ON `(datetime)`
- `idx_major_src` ON `(src)`

**md5 计算差异**:**基于 `title` 字段**(跟 news 的 `content` 不同,2026-09-15 修复版未统一)

**写入规则**:
- 整体一次拉:`tushare pro.major_news(start_date=sd, end_date=ed, limit=2000)`
- 默认 `sd = last_news_ctrl[major_news]` 或 "1 天前 + 00:00:00"
- `ed = now()`
- 断点:`tbl_news_ctrl[major_news]`(pub_time 字符串)
- 每 10 分钟更新一次(高频)

**datetime 来源**:`pub_time` 字段(原样转 datetime 字符串)

---

### 4.3 `tbl_cctv_news` — CCTV 新闻联播(6 列)

| 项 | 值 |
|---|---|
| 主键 | `(datetime, md5)` 复合主键 |
| 写入者 | `CNDataDown.update_cctv_news()` |
| 读取者 | `get_cctv_news()` |
| 数据源 | tushare `pro.cctv_news` |
| 数据行数 | 35,647 |
| 起始日期 | 默认 7 天前(增量) |

**字段(6)**:

| 字段 | 类型 | 语义 |
|---|---|---|
| `datetime` | TEXT PK | **YYYYMMDD**(无时间,cctv 接口无时间字段) |
| `title` | TEXT | 联播标题 |
| `content` | TEXT | 联播全文 |
| `src` | TEXT | 基本固定 `cctv` |
| `md5` | TEXT PK | **未计算**(代码没显式算 md5,主键约束靠 datetime 唯一) |
| `snap_ts` | TEXT | 写入时间 |

**索引**:
- PK: `(datetime, md5)`
- `idx_cctv_date` ON `(datetime)`

**重要:datetime 格式**:**YYYYMMDD 不带时间戳**(跟 news/major_news 完全不同,2026-09-15 修复版未统一)。`get_cctv_news` 不能用 `*_datetime` 参数。

**写入规则**:
- 按日期循环,每天 `tushare pro.cctv_news(date=td)`
- 增量:从 `tbl_news_ctrl[cctv_news]` 的 max_date + 1 开始
- 默认 7 天前(冷启动)
- 一天可能多条(联播 + 偶尔重播/补录),主键 (datetime, md5) 自动去重
- 每天更新一次(全量更新链路第 4 阶段)
- **注意**:`update_cctv_news` **不显式算 md5 字段**,df 里没 md5 列,upsert 时只插 datetime/title/content/src/snap_ts。SQLite 主键检查时 md5 字段为 NULL,实际主键约束退化成只按 datetime 判重(由 `service_cctv_news` 按日期循环的调度保证不重)

---

### 4.4 `tbl_news_ctrl` — 新闻源独立断点

| 项 | 值 |
|---|---|
| 主键 | `src` |
| 写入者 | `_update_news_ctrl_value(conn, src, max_date)`(内部 helper) |
| 读取者 | `get_news_ctrl()` / `_get_news_ctrl_value(conn, src)`(内部 helper) |
| 数据行数 | 11 |

**字段(3)**:

| 字段 | 类型 | 语义 |
|---|---|---|
| `src` | TEXT PK | 源标识 |
| `max_date` | TEXT | 已拉到的最大日期 / datetime(格式按 src 含义) |
| `updated_at` | TEXT | 上次更新时间 ISO |

**11 个 src**:
| src | 格式 | 表 |
|---|---|---|
| `sina` | YYYY-MM-DD HH:MM:SS | tbl_news |
| `wallstreetcn` | YYYY-MM-DD HH:MM:SS | tbl_news |
| `10jqka` | YYYY-MM-DD HH:MM:SS | tbl_news |
| `eastmoney` | YYYY-MM-DD HH:MM:SS | tbl_news |
| `yuncaijing` | disabled | tbl_news |
| `fenghuang` | disabled | tbl_news |
| `jinrongjie` | YYYY-MM-DD HH:MM:SS | tbl_news |
| `cls` | YYYY-MM-DD HH:MM:SS | tbl_news |
| `yicai` | YYYY-MM-DD HH:MM:SS | tbl_news |
| `cctv_news` | YYYYMMDD | tbl_cctv_news |
| `major_news` | YYYY-MM-DD HH:MM:SS | tbl_major_news(pub_time) |

---

## 5. db_cn_index.db (1 张表,2026-09-15 新增)

### 5.1 `tbl_cn_index_basic` — 指数基本信息(全市场指数清单)

| 项 | 值 |
|---|---|
| 主键 | `ts_code` 单字段主键 |
| 写入者 | `CNDataDown.update_index_basic()` |
| 读取者 | `get_index_basic()` |
| 数据源 | tushare `pro.index_basic`(文档 https://tushare.pro/document/2?doc_id=94) |
| 数据行数 | ~8000 行(SW / CSI / MSCI / SSE / SZSE 等全市场指数) |
| 写入方式 | **`replace_table`(全量覆盖,用户要求)** |
| Ctrl key | `cn_index_basic`(basic DB,max_date="all" 表示覆盖完成) |

**字段(9列含 snap_ts)**:

| 字段 | 类型 | 语义 |
|---|---|---|
| `ts_code` | TEXT PK | TS 代码(如 `000001.SH` / `801010.SI`) |
| `name` | TEXT | 简称(如 `上证指数` / `农林牧渔`) |
| `market` | TEXT | 市场(`SW` / `CSI` / `MSCI` / `SSE` / `SZSE`) |
| `publisher` | TEXT | 发布方 |
| `category` | TEXT | 类别(如 `一级行业指数` / `规模指数`) |
| `base_date` | TEXT | 基期 YYYYMMDD |
| `base_point` | REAL | 基点 |
| `list_date` | TEXT | 上市日期 YYYYMMDD |
| `snap_ts` | TEXT | 写入时间 |

**索引**:
- PK: `ts_code`
- `idx_index_market` ON `(market)`

**写入规则**:
- 每次跑全量 ~8000 行,~0.4 秒
- 无日期参数,无增量断点(指数清单是静态的)
- 用户要求每次覆盖更新(`replace_table`)

**调度位置**:**`task_full_update` 阶段 4**(每日 18:00 后跑,~0.4 秒)

**性能**(实测 2026-09-15):
- 单次 8000 行,**0.4s,1 次调用**

---

### 5.2 `tbl_cn_index_daily` — 12 只指数日线行情(2026-09-15 新增)

| 项 | 值 |
|---|---|
| 主键 | `(trade_date, ts_code)` 复合主键 |
| 写入者 | `CNDataDown.update_index_daily()` |
| 读取者 | `get_index_daily()` |
| **数据源平台** | **tushare(pro.tushare.pro)** |
| **API 客户端** | `TushareClient` |
| **接口 endpoint** | `pro.index_daily` |
| 数据行数 | **58,889 行**(12 只指数 × 实际历史天数)|
| 起始日期 | **1993-01-01**(用户要求)|
| 写入方式 | **upsert**(主键 trade_date+ts_code)|
| Ctrl key | `cn_index_daily`(basic DB)|
| DB | `db_cn_index.db`(同 `tbl_cn_index_basic`)|

**12 只指数列表**(`CNDataDown.INDEX_DAILY_CODES`):

| ts_code | 名称 | 实际起始 | 备注 |
|---|---|---|---|
| `000001.SH` | 上证综指 | 1993-01-04 | A 股最早指数,1993 起 |
| `000016.SH` | 上证50 | 2004-01-02 | 上交所 50 |
| `000905.SH` | 中证500 | 2005-01-04 | 中证 500 |
| `000852.SH` | 中证1000 | 2005-01-04 | 中证 1000 |
| `000300.SH` | 沪深300 | 2005-04-08 | 沪深 300 |
| `399001.SZ` | 深证成指 | 1993-01-04 | 深交所成分 |
| `399106.SZ` | 深证综指 | 1993-01-03 | 深交所综合 |
| `399006.SZ` | 创业板指 | 2010-06-01 | 创业板 100 只 |
| `399102.SZ` | 创业板综 | 2010-06-01 | 创业板全部 |
| `000680.SH` | 上证科创板50 | 2019-07-22 | 科创板 |
| `000688.SH` | 科创50 | 2019-07-22 | 科创板 50 |
| `899050.BJ` | 北证50 | 2022-04-29 | 北交所 50 |

> ⚠️ 部分指数**晚于 1993 年成立**(如 000300.SH 2005 年),代码会自动适配,从该指数实际起始日期拉取。

**字段(12,含 snap_ts)**:

| 字段 | 类型 | 语义 |
|---|---|---|
| `trade_date` | TEXT PK | YYYYMMDD |
| `ts_code` | TEXT PK | 指数代码 |
| `close` | REAL | 收盘点位 |
| `open` | REAL | 开盘点位 |
| `high` | REAL | 最高点位 |
| `low` | REAL | 最低点位 |
| `pre_close` | REAL | 昨日收盘点 |
| `change` | REAL | 涨跌点 |
| `pct_chg` | REAL | 涨跌幅(%) |
| `vol` | REAL | 成交量(手) |
| `amount` | REAL | 成交额(千元) |
| `snap_ts` | TEXT | 写入时间 |

**索引**:
- PK: `(trade_date, ts_code)`
- `idx_index_daily_date` ON `(trade_date)`
- `idx_index_daily_code` ON `(ts_code)`

**写入规则**:
- **按 ts_code 循环**(12 只固定指数)
- **每只 ts_code 内 limit + offset 分页**(单只 6000+ 行,limit=6000,offset=0/6000)
- 全量 1993-01-01 ~ 今天,~15 次调用,~5 秒
- 单只指数失败不阻塞其他指数,继续跑

**调度位置**:**`task_full_update` 阶段 4**(每日 18:00 后跑,数据 T+1 更新)

**性能**(实测 2026-09-15):
- 全量 12 只 1993-2026:**58,889 行,5.1s,15 次调用** ✓

### 5.3 `tbl_index_ctrl` — index 库断点表(2026-09-15 新建)

| 项 | 值 |
|---|---|
| 主键 | `key` |
| 写入者 | `update_ctrl_index(conn, key, max_date)` |
| 读取者 | `get_ctrl_index(key)` |
| 数据行数 | 2 |

**字段(3)**:

| 字段 | 类型 | 语义 |
|---|---|---|
| `key` | TEXT PK | 断点 key,见下表 |
| `max_date` | TEXT | 已拉到的最大日期(YYYYMMDD)或 "all"(全量覆盖标记) |
| `updated_at` | TEXT | 上次更新时间 ISO |

**2 个 key**(管 index DB 内的 2 张数据表):
| key | 表 | 类型 |
|---|---|---|
| `cn_index_basic` | tbl_cn_index_basic | "all"(全量覆盖,~0.4 秒) |
| `cn_index_daily` | tbl_cn_index_daily | YYYYMMDD(12 只指数日线行情) |

**设计原则**(2026-09-15 新建):**本地管理 index DB 数据**。

---

## 6. Schema 声明位置

`scripts/config/settings.py`:

```python
SCHEMA_SQL_BASIC  # 7 张表(basic 6 数据 + 1 ctrl)
SCHEMA_SQL_KPL    # 3 张表(kpl 3 数据,kpl_ctrl 已废弃)
SCHEMA_SQL_NEWS   # 4 张表(news 3 数据 + 1 ctrl)
SCHEMA_SQL        # 兼容旧代码,= SCHEMA_SQL_BASIC
DB_PATH_BASIC, DB_PATH_KPL, DB_PATH_NEWS  # 三个 DB 文件路径
```

`init_db(db_name)` 在 `core/offline_db_client.py`:
- `db_name` ∈ {`basic`, `kpl`, `news`}
- 自动建表(`CREATE TABLE IF NOT EXISTS`)
- 启用 WAL 模式(`PRAGMA journal_mode=WAL`)

`CNDataDown.__init__()` 一次性初始化 3 个 conn:
```python
self.conn_basic = init_db("basic", verbose=False)
self.conn_kpl = init_db("kpl", verbose=False)
self.conn_news = init_db("news", verbose=False)
self.conn = self.conn_basic  # 兼容旧代码
```

---

## 6. 数据完整性约束

- **主键去重**:`upsert_df(conn, df, table, key_cols=...)` 用 `INSERT OR IGNORE`,主键冲突行跳过(等价幂等)
- **多日表**:week / month 用 `replace_table`(DELETE + INSERT 事务化),`last_success_date` 只在有 INSERT 时推进
- **断点推进**:每张表的断点只在实际写入成功后更新(`if inserted > 0: update_ctrl`)
- **空日不推进**:`update_kpl_list` / `update_kpl_limit_performance` / `update_daily` 等循环里,空日(`df is None or len(df) == 0`)`continue` 但**不更新断点**

---

## 7. 一句话总结

**3 个 SQLite 库,29 张表 → 实际是 4 个 SQLite 库(basic/kpl/news/index),31 张表(含 4 个 ctrl 断点表 + 27 张数据表),主键 + 索引清晰,basic 库 13 数据表承担主要行情 + 资金流向 + 融资融券汇总 + 融资融券明细 + 筹码及胜率 + 每日指标 + 统一 ctrl,kpl 库 9 数据表承担涨停 + 龙虎榜 + 港股通 + 涨跌停列表 + 本地 ctrl,news 库 3 数据表承担 11 源新闻聚合 + 源 ctrl,index 库 2 数据表承担指数基本信息 + 12 只指数日线行情,所有表都有 `snap_ts` 字段记录写入时间(2026-09-15 加 index DB + 重建 kpl DB 本地 ctrl)。**
