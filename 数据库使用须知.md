# 数据库使用须知

> **目标读者**:项目合作伙伴(拿到 GitHub 邀请后)
> **作用**:告诉你项目用哪些 SQLite 数据库 / 各自存什么数据 / **应该放哪里** / 怎么验证

---

## 1. ⚠️ 重要前提

**本项目 GitHub 仓库是 private + 100 MB 文件大小限制**,所以:

- **代码 + 文档** 在 GitHub 上(`LiYiqiang0827/TradingAgent`)
- **21 GB 数据库** 不在 GitHub 上,需要从**百度网盘**单独下载
- **本仓库不存任何 db 文件**(`.gitignore` 排除了 `**/data/`)

---

## 2. 数据库清单(7 个 .db 文件,总 ~28 GB)

### 2.1 离线数据库(`offlineDataManager/data/`)

| 文件 | 大小 | 包含数据 |
|---|---|---|
| `db_cn_basic.db` | **13 GB** | 股票基本信息 + 日 K + 周 K + 月 K + 涨跌停 + 复权因子 + 资金流 + 融资融券 + 筹码 + 财务等 14 张表 |
| `db_cn_kpl.db` | **1.9 GB** | KPL 涨停榜 + 题材 + 龙虎榜 + 大宗交易 + 沪深港通等 8 张表 |
| `db_cn_index.db` | **13 MB** | 指数基本信息 + 指数日线 2 张表 |
| `db_cn_news.db` | **6.4 GB** | 普通新闻 + 头条新闻 + CCTV 新闻 3 张表 |
| `policy_minute.db` | **1.3 GB** | 个股分钟 K(涨停研究用) + 5 指数分钟 K |
| `policy_ticks.db` | **4.9 GB** | 个股逐笔成交(涨停研究用) |

### 2.2 实时数据库(`onlineDataManager/data/`)

| 文件 | 大小 | 包含数据 |
|---|---|---|
| `online_data_<YYYYMM>.db` | ~176 MB / 月 | 实时行情快照 / 5 档盘口 / 分钟 K / 集合竞价 / 异动(每月一个文件) |

> 当前已有 `online_data_202609.db`(2026 年 9 月)

---

## 3. 数据库放置路径(⚠️ 严格)

clone 完项目后,**数据库必须放在以下路径**(不是项目外的目录):

```
~/TradingAgent/
├── offlineDataManager/
│   └── data/                                       ← ⚠️ 这 6 个 db 放这里
│       ├── db_cn_basic.db                          (13GB)
│       ├── db_cn_kpl.db                            (1.9GB)
│       ├── db_cn_index.db                          (13MB)
│       ├── db_cn_news.db                           (6.4GB)
│       ├── policy_minute.db                        (1.3GB)
│       └── policy_ticks.db                         (4.9GB)
│
└── onlineDataManager/
    └── data/                                       ← ⚠️ 实时 db 放这里
        └── online_data_202609.db                   (176MB)
```

**为什么必须放这里?**

代码里 `offlineDataManager/scripts/config/settings.py` 写死了路径:
```python
DB_PATH_BASIC = Path(__file__).parent.parent.parent / 'data' / 'db_cn_basic.db'
# 实际路径:~/TradingAgent/offlineDataManager/data/db_cn_basic.db
```

**放错位置** → 代码找不到 db → 报 `FileNotFoundError` 或 `sqlite3.OperationalError`。

---

## 4. 从百度网盘下载(等 owner 上传后)

> 📌 **截至 2026-09-18**,百度网盘链接**还未生成**(owner 还在上传)。
> 拿到链接后,本节步骤会更新。

### 4.1 拿到分享链接后,操作步骤

1. **复制分享链接**:
   ```
   https://pan.baidu.com/s/1xxxxxxxxx  提取码:xxxx
   ```

2. **用 rclone / 浏览器 / baidu-pcs-go 下载**:
   ```bash
   # rclone 配置百度网盘(只配一次,详见 CONTRIBUTING.md):
   rclone config  # 新建 remote,名字: baidu

   # 下载整个 TradingAgent-data 目录
   rclone copy baidu:TradingAgent-data ~/TradingAgent --progress
   ```

   或者浏览器:
   - 打开链接,勾选 7 个 `.db` 文件
   - 下载到 `~/Downloads/`
   - 用下面 4.2 的命令移动到正确位置

3. **如果是朋友**直接给的(没有网盘):
   - 用 U 盘 / 飞秋 / 微信传文件(7 个 .db,28GB)
   - 移动到上述路径

### 4.2 移动到正确位置(假设下载到 `~/Downloads/`)

```bash
cd ~/TradingAgent

# 离线 6 个 db
mv ~/Downloads/db_cn_basic.db    offlineDataManager/data/
mv ~/Downloads/db_cn_kpl.db      offlineDataManager/data/
mv ~/Downloads/db_cn_index.db    offlineDataManager/data/
mv ~/Downloads/db_cn_news.db     offlineDataManager/data/
mv ~/Downloads/policy_minute.db  offlineDataManager/data/
mv ~/Downloads/policy_ticks.db   offlineDataManager/data/

# 实时 db(放在月份子目录,如果有)
mkdir -p onlineDataManager/data/202609
mv ~/Downloads/online_data_202609.db onlineDataManager/data/
```

或者一键:
```bash
mv ~/Downloads/*.db ~/TradingAgent/offlineDataManager/data/
mv ~/Downloads/online_data_*.db ~/TradingAgent/onlineDataManager/data/
```

### 4.3 验证下载完整性

```bash
cd ~/TradingAgent
ls -lh offlineDataManager/data/*.db onlineDataManager/data/*.db
```

**对照预期大小**(2026-09-18 快照):
- `db_cn_basic.db`    ≈ 13 GB
- `db_cn_kpl.db`      ≈ 1.9 GB
- `db_cn_index.db`    ≈ 13 MB
- `db_cn_news.db`     ≈ 6.4 GB
- `policy_minute.db`  ≈ 1.3 GB
- `policy_ticks.db`   ≈ 4.9 GB
- `online_data_202609.db` ≈ 176 MB

**差异 > 5% 可能是下载损坏** —— 重新下载。

### 4.4 验证 db 完整性(跑 SQL 查表行数)

```bash
cd ~/TradingAgent

# 基础信息
sqlite3 offlineDataManager/data/db_cn_basic.db \
    "SELECT COUNT(*) FROM tbl_cn_basic;"
# 期望:~5,565 行

# 日 K
sqlite3 offlineDataManager/data/db_cn_basic.db \
    "SELECT COUNT(*) FROM tbl_cn_day;"
# 期望:~11,000,000 行

# KPL 涨停榜
sqlite3 offlineDataManager/data/db_cn_kpl.db \
    "SELECT COUNT(*) FROM tbl_cn_kpl_list;"
# 期望:~184,898 行
```

如果行数为 0,说明 db 是空的或损坏。

### 4.5 验证 data_provider 跑通

```bash
cd ~/TradingAgent
PYTHONPATH=. python3 -c "
from coreClient.data_provider import (
    get_basic, get_kpl_limit_performance, get_kpl_list, get_news,
)

# 基础信息
print('get_basic:', len(get_basic(exchange='SSE', list_status='L')), '行')

# KPL 涨停详情
print('get_kpl_limit_performance:', 
      len(get_kpl_limit_performance(trade_date='2026-09-17', source='database')), '行')

# 新闻
print('get_news:', 
      len(get_news(start_date='2026-09-10', end_date='2026-09-17', source='database')), '行')
"
```

期望输出:
```
get_basic: 5565 行
get_kpl_limit_performance: ~30 行(每天涨停数)
get_news: ~10000+ 行(一周 9 源聚合)
```

**行数严重不符** → 重新检查 db 放置路径 / 完整性。

---

## 5. 注意事项

### 5.1 不要修改 db 内容

- db 是**只读数据快照**(从 tushare / kpl / tdx 下载的)
- **严禁手动 SQL 改 db** —— 会破坏 schema / 索引 / 同步状态
- 修改需求请用 `coreClient.data_provider` 接口,或提 PR 加新方法

### 5.2 db 会持续更新

- **每周 owner 跑一次全量更新**(`scheduler_updateData`)
- **每日增量**由 `service_*` 进程完成
- 你的本地 db 是**某个时间点的快照**,会**逐渐过期**(数据是补的)
- 想用最新数据 → 联系 owner 重发网盘链接 / 或自己跑 `service_*` 增量

### 5.3 db 版本兼容

代码里 `settings.py` 定义了 schema,如果你的 db schema 跟代码不匹配会报错:
```
sqlite3.OperationalError: no such column: xxx
```

**解决**:
- 让 owner 发跟你代码版本对应的 db
- 或升级你的代码 `git pull` 到对应 db 的版本

### 5.4 大小写 / 文件名

- **文件名严格区分大小写**:`db_cn_basic.db` ≠ `db_cn_Basic.db`
- **不要** 改文件名(代码硬编码)
- macOS 默认大小写不敏感,可能会踩坑

---

## 6. 不需要下载的 db(选下,节省时间)

如果你只是跑 demo / 看代码,**最小集**:

| 必须 | 文件 | 大小 |
|---|---|---|
| ⭐ | `db_cn_basic.db` | 13 GB(基础信息 + 日 K) |
| ⭐ | `db_cn_index.db` | 13 MB(指数) |
| ⭐ | `db_cn_kpl.db` | 1.9 GB(涨停研究) |
| | `db_cn_news.db` | 6.4 GB(新闻,可选) |
| | `policy_minute.db` | 1.3 GB(分钟 K,策略用) |
| | `policy_ticks.db` | 4.9 GB(逐笔,策略用) |
| | `online_data_202609.db` | 176 MB(实时) |

**最小集 = 15 GB**(3 个 db),能跑通 80% 的数据接口测试。

---

## 7. 完整工作流

```bash
# 1. 拿到项目 owner 的 GitHub 邀请
# 2. clone 仓库
cd ~/
git clone git@github.com:LiYiqiang0827/TradingAgent.git
cd TradingAgent

# 3. 准备环境
conda create -n trading python=3.12
conda activate trading
pip install pandas numpy loguru tushare redis requests pytdx

# 4. 拿百度网盘分享链接(联系 owner)
# 5. 下载 db 到正确位置(见 §3)
rclone copy baidu:TradingAgent-data ~/TradingAgent --progress

# 6. 验证
PYTHONPATH=. python3 -c "
from coreClient.data_provider import get_kpl_limit_performance
print(len(get_kpl_limit_performance(trade_date='2026-09-17', source='database')), '行')
"

# 7. 跑 demo
PYTHONPATH=. python3 -c "
from coreClient.data_provider import get_kpl_list
df = get_kpl_list(trade_date='2026-09-17', tags='涨停')
print(df.head())
"
```

---

## 8. 变更历史

| 日期 | 变更 |
|---|---|
| 2026-09-18 | v1.0 初版(百度网盘待上传,本节步骤会更新) |